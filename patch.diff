---

# 2) Code patch for v1.2

Apply these diffs (from repo root) with `git apply` or patch manually.

### A) `pyproject.toml` — bump to 1.2.0
```diff
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -3,7 +3,7 @@ build-backend = "setuptools.build_meta"

 [project]
 name = "cisco-hashgen"
-version = "1.1.1"
+version = "1.2.0"
 description = "Generate and verify Cisco-compatible PBKDF2 password hashes (ASA & IOS/IOS-XE)"
 readme = "README.md"
 requires-python = ">=3.8"
@@ -26,6 +26,7 @@ Issues = "https://github.com/Krontab/cisco-hashgen/issues"

 [project.scripts]
 cisco-hashgen = "cisco_hashgen.cli:main"
